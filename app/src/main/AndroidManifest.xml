<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qltv">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".managerReader.UserManagerActivity"></activity>
        <activity android:name=".ui.AddDeleteBookActivity" />
        <activity android:name=".managerBook.StatisticalActivity" />
        <activity
            android:name=".managerBook.ManagerBookDrActivity"
            android:theme="@style/Theme.MyFancyTheme" />
        <activity
            android:name=".ui.DetailActivity"
            android:theme="@style/Theme.MyFancyTheme" />
        <activity android:name=".ui.ManagerBookActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"></activity>
    </application>

</manifest>